export interface CharacterProps {
  firstName: string;
  lastName: string;
  location?: string;
  gender: string;
  dateOfBirth: string;
  groups?: string[];
  phoneNumber?: string;
  lastPlayed: string;
}
